<p style="font-size:16px; color:#ffffff; font-weight:900; letter-spacing:0.08em; margin:0 0 8px;">
  <% if @reason == "match" %>
    New Campaign Match
  <% else %>
    New Campaign Live
  <% end %>
</p>

<p>Hey <%= @user.name.presence || "adventurer" %>,</p>

<p style="color:#94a3b8; margin:0 0 12px;">
  <% if @reason == "match" %>
    A campaign aligned with your preferences just opened.
  <% else %>
    A campaign is now live on the board.
  <% end %>
</p>

<p style="margin:0 0 12px;">
  <span style="display:inline-block; padding:4px 8px; border:2px solid #4f46e5; color:#a5b4fc; font-size:10px; font-weight:900; text-transform:uppercase;"><%= @campaign.status %></span>
</p>

<table width="100%" cellpadding="0" cellspacing="0" role="presentation" style="margin:12px 0 0; border-top:1px solid #1f2937;">
  <tr>
    <td style="padding:6px 0; font-weight:900;"><%= @campaign.title %></td>
  </tr>
  <tr>
    <td style="padding:6px 0;">Atmosphere: <%= @campaign.atmosphere.presence || "TBD" %></td>
  </tr>
  <tr>
    <td style="padding:6px 0;">Start time: <%= @campaign.start_time.presence || "TBD" %></td>
  </tr>
  <tr>
    <td style="padding:6px 0;">Needs: <%= @campaign.needs.presence&.join(", ") || "TBD" %></td>
  </tr>
</table>

<p>Check QuestBoard to request a seat or invite others.</p>
